# Patrick Agent — reference implementation built on local-agent-kit.
#
# Install: pip install -r requirements.txt
#
# Pinned to the kit's v0.4.0 tag — this repo is a working snapshot and
# must not break when the kit's main branch evolves. Cloud search and
# Telegram are vendored in patrick_agent/ (the kit dropped both in
# v0.4.0 to stay zero-cloud-account).

local-agent-kit @ git+https://github.com/bene-art/local-agent-kit@v0.4.0

# HTTP — for Telegram, Gemini, Alpaca, Odds API.
aiohttp>=3.9
httpx>=0.27
requests>=2.31

# YAML — agent.yaml config.
pyyaml>=6.0
