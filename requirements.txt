# Patrick Agent — reference implementation built on local-agent-kit.
#
# Install: pip install -r requirements.txt
#
# Pin to a tagged release of local-agent-kit once one ships; for now we
# track the main branch.

local-agent-kit @ git+https://github.com/bene-art/local-agent-kit@main

# HTTP — for Telegram, Gemini, Alpaca, Odds API.
aiohttp>=3.9
httpx>=0.27
requests>=2.31

# YAML — agent.yaml config.
pyyaml>=6.0
